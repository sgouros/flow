- @navigation_path = t(:search)
#document_search
  = form_tag search_results_path, :method=>'get' do
    = text_field_tag  :search, params[:search],  :id => 'search_field', :class => 'search_input', :placeholder => t(:title_or_protocol)
    = submit_tag t(:search), :name => nil, :class => 'formbutton'
