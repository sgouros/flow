- @navigation_path= t(:archived_documents)
#documents
  - if @documents
    = paginate @documents
    - @documents.each do |doc|
      %p
        =  link_to doc.full_title, document_path(doc)
    = paginate @documents
  - else
    %p= t :no_documents_exist
